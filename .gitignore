# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files — never commit secrets
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
npm-debug.log*
next-env.d.ts
*.tsbuildinfo

# generated service worker (rebuilt on every `next build`)
public/sw.js
public/sw.js.map
public/swe-worker.js*

# Freebuff local desktop cache (never merge)
.freebuff/
desktop.db
desktop.db-shm
desktop.db-wal
desktop-v2.db
